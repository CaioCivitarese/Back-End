const minutos = 60;
const horas = minutos * 60;
const dias = horas * 24;
const ano = dias * 365;

console.log("quantos minutos temos em uma hora:", horas);
console.log("quantos minutos temos em um dia:", dias);
console.log("quantos minutos temos em um ano:", ano);